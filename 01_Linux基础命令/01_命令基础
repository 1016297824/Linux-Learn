// 命令基础

1.命令通用格式
    command [-options] [parameter]
    (1)command:命令本身
    (2)-options:命令的一些选项，用于控制命令的行为细节
    (3)parameter:参数，多数为指向的目标等

2.路径
(1)绝对路径：以根目录为起点
(2)相对路径：以当前目录为起点
(3)特殊路径符号
    ".":表示当前目录，如 cd ./Desktop
    "..":表示上一级目录，如 cd ../..进入上两级
    "~":表示Home目录，如 cd ~/Desktop

3.通配符"*"：用来做模糊匹配
例子：
    test*:匹配任何以test开头的内容
    *test:匹配任何以test结尾的内容
    *test*:匹配任何包含test的内容