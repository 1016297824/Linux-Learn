// which,find命令

1.which命令：查看使用的一系列命令的程序文件存放位置
(1)语法：which [要查找的命令]

2.find命令：搜索指定文件
(1)语法：
    1)find [起始路径] -name ["被查找文件名"]
    2)find [起始路径] -size [+|-n][kMG]
(2)选项：
    1)-name:按名称查找
    2)-size:按大小查找
        +,-:标识大于和小于
        n:表示数字大小
        kMG:表示大小单位，k->kb / M->MB / G->GB
    3)选项可以混用
(3)注意：被查找的文件最好用双引号括起来，否则可能会被系统解释为特殊字符
