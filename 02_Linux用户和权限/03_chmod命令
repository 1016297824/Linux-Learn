// chmod命令:chmod [-R] 权限 文件或文件夹

1.chmod命令：修改文件或文件夹权限信息
(1)选项：
    1)-R:对文件夹内全部内容应用同样操作
(2)例子：
    1)chmod u=rwx,g=rx,o=x [文件]
        将文件权限修改为：rwxr-x--x
    2)chmod -R u=rwx,g=rw,o=x [文件夹]
        将文件夹及其内全部文件的权限设置为：rwxr-x--x
    3)chmod 751 [文件]
        将文件权限修改为：rwxr-x--x
