// 文件压缩

1.tar命令：解压压缩文件（tar或tar.gz格式）
(1)语法：tar [-c -v -x -f -z -C] [参数1] [参数2] ... [参数N]
(2)选项：
    1)-c:创建压缩文件
    2)-v:显示压缩、解压过程，用于查看进度
    3)-x:解压模式
    4)-f:要创建的文件或要压缩的文件，必须位于最后一个
    5)-z:gzip模式
    6)-C:选择解压目的地

2.zip命令：压缩文件（zip格式）
(1)语法：zip [-r] [参数1] [参数2] ... [参数N]
(2)选项：
    1)-r:被压缩的内容包含文件夹时使用
    
3.unzip命令：解压文件（zip格式）
(1)语法：unzip [-d] [参数]
(2)选项：
    1)-d:指定要解压去的位置