// 端口

1.端口分类：
(1)公认端口：1~1023，通常用于一些系统内置或知名程序的预留使用
(2)注册端口：1024~49151，通常可随意使用，用于松散的绑定一些程序/服务
(3)动态端口：49152~65535，通常不会固定绑定程序，而当程序对外进行网络连接时，用于临时使用

2.nmap命令
(1)安装：yum -y install nmap
(2)语法：nmap [被查看IP地址]

3.netstat命令：
(1)安装：yum -y install net-tools
(2)语法：netstat -anp | grep [端口号]
