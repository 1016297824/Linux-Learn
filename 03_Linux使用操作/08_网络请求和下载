// 网络请求和下载

1.ping命令：
(1)语法：ping [-c num] [ip或主机名]
(2)选项：
    1)-c:检查次数

2.wget命令：非交互式文件下载器，可以在命令行内下载网络文件
(1)语法：wget [-b] [url]
(2)选项：
    1)-b:后台下载，同时会将日志写入到当前工作目录的wget-log文件

3.curl命令：可以发送http网络请求，可用于 下载文件、获取信息等
(1)语法：curl [-O] [url]
(2)选项：
    1)-O:用于下载文件，当url是下载链接时，可以使用此选项保存文件
